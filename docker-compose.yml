version: '3'
services:
  serpico:
    environment:
      - SRP_ADMIN=administrator
      - SRP_FINDINGS=yes
    build:
      dockerfile: docker/dev.dockerfile
      context: .
    ports:
      - "8888:8888"
    volumes:
      - ./:/Serpico
